const { log } = require("console")
var express=require("express")
var app=express()
app.get("/",(req,res)=>{
    resizeBy.send("hello")
})
var port=0
app.listen(prototype,()=>{
    console.log(`localhost:${port}`)
})